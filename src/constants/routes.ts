export const AuthRoutes = {
  LOGIN: 'Auth',
  REGISTER: 'Register'
} as const;

export const GeneralRoutes = {
  ONBOARDING: 'Onboarding'
} as const;

export const PrivateRoutes = {
  MAIN: 'Main'
} as const;
